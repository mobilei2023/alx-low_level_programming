#include "main.h"
/**
 * _isupper - Checks for  uppercase letters
 * @C: char to  check
 *
 * Return: 1 if character is uppercase, 0 otherwise.
 */

int _isupper(int C);
{
	if (C >= "A" && C <= "Z")
		return (1);
}
	else
		return (0);
		}
}
